<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>청원 페이지</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">숭실대학교 학생청원 사이트</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link <%= (activePage || '') === 'login' ? 'active' : '' %>" href="/auth/login">로그인</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= (activePage || '') === 'petition' ? 'active' : '' %>" href="/petitions/create">청원하기</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container text-center mt-4">
        <img src="/img/숭실대학교로고.jpg" class="logo-image" alt="숭실대학교 로고">
    </div>

    <div class="container mt-5">
        <h2 class="text-center mb-4">진행 중인 청원 목록</h2>
        <!-- 청원 목록 -->
        <ul class="petition-list" id="petitionList">
            <% if (petitions && petitions.length > 0) { %>
                <% petitions.forEach(function(petition) { %>
                    <li><%= petition.title %></li>
                <% }) %>
            <% } else { %>
                <li>현재 진행 중인 청원이 없습니다.</li>
            <% } %>
        </ul>
    </div>

    <script src="/script.js"></script> <!-- 외부 JavaScript 파일 연결 -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
